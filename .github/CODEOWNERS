# Every change to this repo's workflows/hooks is reviewed -- it's called by every
# property and client repo in the org, so a bad change here is a bad change
# everywhere at once. Two real identities, not one: James ruled "you plus one"
# before this repo goes public, precisely because @jamesd-cloud was the sole
# account behind every agent's commits, making a single-owner CODEOWNERS review
# a formality rather than a real second look. @megr-cloud is Meg's own account,
# a genuinely separate principal -- named here 2026-09-18, closing that TODO.
* @jamesd-cloud @megr-cloud
